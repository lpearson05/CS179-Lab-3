$(document).ready(function() {  // wait for DOM ready event

    // grab a reference to the form
    var form = $("#addpost");
    
    // bind to the form's 'submit' event
    
        // get user input
        
        // send the data to 'add_post.php'
        // make an ajax call that posts to 'add_post.php'
        // send data:'url','description','title'

        // create a post
        // 1. create a new 'div' with class 'post'
        // 2. create a new 'a' linking to "http://"+<user's url>
        //      and text <user's title>
        // 3. append the 'a' and " - <user-s description"
        //      to the 'div.post'
            
        // get a reference to the list of posts
        var list = $("div#posts-list");
            
        // show the post to the user by
        // adding the post to the post list

        return false; // prevent page refresh
    

});